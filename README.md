# COMP5700-Project

Group Members: Josh Jelich (jdj0056)

Libraries Used: Gemma-3-1b (LLM), os, re, yaml, json, ollama, pdfplumber

Task 1 runs completely automated but may take some time to create the yaml files.

Tasks 2 and 3 run 'for' loops to allow the user to run all combinations with one run of the file (expect 9, but can run any amount).

For tasks 2 and 3, the expected input should only be numerical values, any other input could cause unexpected bahavior.

Task 2 prompts the user for how many comparisons they would like to run and which two yaml files they would like to compare on the specific run. It should run quickly. 

Task 3 also prompts the user for how many comparisons they would like to run and then automatically runs all comparisons. This could also take some time.
